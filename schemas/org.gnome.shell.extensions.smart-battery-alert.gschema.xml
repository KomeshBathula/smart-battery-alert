<?xml version="1.0" encoding="UTF-8"?>
<schemalist>
  <schema id="org.gnome.shell.extensions.smart-battery-alert" path="/org/gnome/shell/extensions/smart-battery-alert/">

    <!-- General -->
    <key name="update-time" type="i">
      <default>30</default>
      <summary>Fallback polling interval (seconds)</summary>
      <description>Seconds between fallback battery polls. Primary updates come from D-Bus signals.</description>
    </key>
    <key name="show-panel-percentage" type="b">
      <default>true</default>
      <summary>Show battery percentage in panel</summary>
      <description>Display the current battery percentage next to the icon in the top bar.</description>
    </key>

    <!-- Low Battery -->
    <key name="enable-low-battery-alarm" type="b">
      <default>true</default>
      <summary>Enable low battery alerts</summary>
      <description>Show notifications when battery drops below the low battery threshold.</description>
    </key>
    <key name="low-battery-threshold" type="i">
      <default>30</default>
      <range min="15" max="50"/>
      <summary>Low battery threshold (%)</summary>
      <description>Battery percentage at which low battery alerts begin.</description>
    </key>
    <key name="critical-battery-threshold" type="i">
      <default>20</default>
      <range min="5" max="25"/>
      <summary>Critical battery threshold (%)</summary>
      <description>Battery percentage at which the persistent critical alert appears.</description>
    </key>
    <key name="low-battery-alert-interval" type="i">
      <default>2</default>
      <range min="1" max="5"/>
      <summary>Low battery alert interval (%)</summary>
      <description>Alert the user for every N% decrease in battery below the threshold.</description>
    </key>

    <!-- Charge Limit -->
    <key name="enable-charge-limit-alarm" type="b">
      <default>true</default>
      <summary>Enable charge limit alarm</summary>
      <description>Alert when battery reaches the configured charge limit while charging.</description>
    </key>
    <key name="charge-limit" type="i">
      <default>80</default>
      <range min="60" max="100"/>
      <summary>Battery charge limit (%)</summary>
      <description>Battery charge level at which to alert the user to unplug.</description>
    </key>

    <!-- Charge Prediction -->
    <key name="enable-charge-prediction" type="b">
      <default>true</default>
      <summary>Enable charge time prediction</summary>
      <description>Show estimated time when the battery will be fully charged.</description>
    </key>

    <!-- Battery Health Tracking -->
    <key name="battery-health-log" type="s">
      <default>'{}'</default>
      <summary>Battery health history</summary>
      <description>JSON log of battery health degradation over time.</description>
    </key>
    <key name="charge-cycle-count" type="i">
      <default>0</default>
      <summary>Charge cycle count</summary>
      <description>Number of full charge cycles completed.</description>
    </key>
    <key name="health-warning-threshold" type="i">
      <default>80</default>
      <range min="50" max="95"/>
      <summary>Battery health warning threshold (%)</summary>
      <description>Show warning when battery health drops below this percentage.</description>
    </key>
    <key name="last-full-capacity" type="d">
      <default>0.0</default>
      <summary>Last recorded full capacity (Wh)</summary>
      <description>Most recent full capacity measurement for tracking degradation.</description>
    </key>

    <!-- Usage Statistics -->
    <key name="usage-stats" type="s">
      <default>'{}'</default>
      <summary>Usage statistics</summary>
      <description>JSON log of battery usage patterns and statistics.</description>
    </key>

    <!-- Sound Alerts -->
    <key name="enable-sound-alerts" type="b">
      <default>true</default>
      <summary>Enable sound alerts</summary>
      <description>Play sounds for battery notifications.</description>
    </key>
    <key name="sound-volume" type="i">
      <default>50</default>
      <range min="0" max="100"/>
      <summary>Alert sound volume (0-100)</summary>
      <description>Volume level for battery alert sounds.</description>
    </key>
    <key name="low-battery-sound" type="s">
      <default>'battery-low'</default>
      <summary>Low battery sound</summary>
      <description>Sound theme name for low battery alerts.</description>
    </key>
    <key name="charge-limit-sound" type="s">
      <default>'complete'</default>
      <summary>Charge limit sound</summary>
      <description>Sound theme name for charge limit alerts.</description>
    </key>
    <key name="full-charge-sound" type="s">
      <default>'complete'</default>
      <summary>Full charge sound</summary>
      <description>Sound theme name for full charge alerts.</description>
    </key>

  </schema>
</schemalist>
